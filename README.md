# Open Folder by Path

Open a local folder by typing its path.

## Usage

Run the `Open Folder by Path` command and type a folder path.

- `Enter` opens the current input path or the active folder match.
- `Tab` completes the active folder match and keeps the picker open.

The picker starts from the current workspace folder when one is open, and falls
back to the home folder otherwise.
